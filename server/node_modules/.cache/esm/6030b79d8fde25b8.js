let express;_8fd‍.w("express",[["default",["express"],function(v){express=v}]]);

const router = express.Router();

router.get("/:message", (req, res) => {
  res.status(200).send(`Here is your message: ${req.params.message}`);
});

module.exports = router;
